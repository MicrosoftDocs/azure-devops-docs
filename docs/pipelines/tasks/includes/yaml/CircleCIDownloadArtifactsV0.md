```YAML
# Download Artifacts - CircleCI
# Download artifacts from CircleCI build
- task: DownloadCircleCIArtifacts@0
  inputs:
    connection: 
    definition: 
    version: 
    #itemPattern: '**' # Optional
    #downloadPath: '$(System.ArtifactsDirectory)' 
    #downloadCommitsAndWorkItems: true # Optional
```
