```YAML
# Docker CLI installer
# Install Docker CLI on agent machine.
- task: DockerInstaller@0
  inputs:
    #dockerVersion: '17.09.0-ce' 
    #releaseType: 'stable' # Optional. Options: stable, edge, test, nightly
```
