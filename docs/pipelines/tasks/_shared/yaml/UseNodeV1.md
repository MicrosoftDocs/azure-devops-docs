```YAML
# Use Node.js ecosystem
# Set up a Node.js environment and add it to the PATH, additionally providing proxy support
- task: UseNode@1
  inputs:
    #version: '10.x' # Optional
    #checkLatest: false # Optional
```
